{# app/views/_includes/special-appointment-warning-card.njk #}

{% if appointment | isSpecialAppointment %}
  {% set appointmentUrl = "/clinics/" + clinicId + "/appointments/" + appointmentId %}
  {% set changeHref = (appointmentUrl + "/special-appointment/edit") | urlWithReferrer(currentUrl) %}

  {% call warningCallout({
    heading: "Special appointment",
    headingLevel: 2,
    classes: "nhsuk-u-margin-bottom-4",
    actions: {
      items: [
        {
          href: changeHref,
          text: "Change",
          visuallyHiddenText: "special appointment requirements"
        } | openInModal
      ]
    }
  }) %}
    {% set summaryListClasses = "nhsuk-summary-list" %}
    {% include "summary-lists/special-appointment/special-appointment.njk" %}
  {% endcall %}
{% endif %}